package main

func main() {
	//x := primitivetypes.CountBits(222)
	//fmt.Println(x)
	//fmt.Println(strconv.FormatInt(222, 2))
	//fmt.Printf("%b\n", 0b11011110)

	/*pathName := path("/usr/bin/tso") // Conversion from string to path.
	pathName.TruncateAtFinalSlash()
	fmt.Printf("%s\n", pathName)

	slice := make([]int, 0, 5)
	for i := 0; i < 10; i++ {
		slice = Extend(slice, i)
		fmt.Printf("len=%d cap=%d slice=%v\n", len(slice), cap(slice), slice)
		fmt.Println("address of 0th element:", &slice[0])
	}

	slice1 := []int{0, 1, 2, 3, 4}
	slice2 := []int{55, 66, 77}
	fmt.Println(slice1)
	slice1 = Append(slice1, slice2...) // The '...' is essential!
	fmt.Println(slice1)*/

	/*
		[3,1,2,1,4,1,2,2,5,3,2,1,1,4,5,2,3,2,5,5]
		[2,4,1,2,3,4,2,4,5,5,1,1,2,1,1,1,5,4,1,4,2,1,5,4,2,3,1,5,2,1]

		[4,4,4,2,4,1,4,3,2,4,4,2,2,2,5,2,5,4,5,1]
		[2,1,5,4,2,3,5,1,2,2]
	*/

	/*
	 */
	//a := []int{3, 1, 2, 1, 4, 1, 2, 2, 5, 3, 2, 1, 1, 4, 5, 2, 3, 2, 5, 5}
	//b := []int{2, 4, 1, 2, 3, 4, 2, 4, 5, 5, 1, 1, 2, 1, 1, 1, 5, 4, 1, 4, 2, 1, 5, 4, 2, 3, 1, 5, 2, 1}

	//maxUncrossedLines(a, b)
	//res := slidingWindow(5, a)
	//for _, _ = range res {
	//}

	//input := [][]int{{5, 3}, {1, 0}, {3, 2}, {3, 0}, {4, 1}, {4, 3}, {3, 1}, {5, 4}, {5, 2}, {2, 0}}

	//possibleBipartition(10, input)
	//findOrder(6, input)
	/*
		// Create a couple of starter slices.
		slice := []int{1, 2, 3}
		slice2 := []int{55, 66, 77}
		fmt.Println("Start slice: ", slice)
		fmt.Println("Start slice2:", slice2)

		// Add an item to a slice.
		slice = append(slice, 4)
		fmt.Println("Add one item:", slice)

		// Add one slice to another.
		slice = append(slice, slice2...)
		fmt.Println("Add one slice:", slice)

		// Make a copy of a slice (of int).
		slice3 := append([]int{}, slice...)
		fmt.Println("Copy a slice:", slice3)

		// Copy a slice to the end of itself.
		fmt.Println("Before append to self:", slice)
		slice = append(slice, slice...)
		fmt.Println("After append to self:", slice)
	*/
}
